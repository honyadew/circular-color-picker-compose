pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "CircularColorPicker"
include(":app")
include(":harmony-color-picker")
